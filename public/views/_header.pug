header
  nav.nav__top
    img.nav__top--logo(src="/img/my-logo.png")
    ul.nav__top--list
      li: a(href="/dashboard") Dashboard
      if user
        li: a(href="/play") Play 
        li: .logout(onclick='checkLogout()')= user.username
      else 
        li: a(href="/login") Login

